import 'package:flutter/material.dart';

class AppColor {
  //───────────────────────────────────
  // Light Theme Colors
  //───────────────────────────────────
  static const Color primary = Colors.deepPurple;
  static const Color textLightPrimary = Colors.white;
  static const Color snackBarLight = Colors.deepPurple;

  //───────────────────────────────────
  // Dark Theme Colors
  //───────────────────────────────────
  static const Color primaryDark = Colors.deepPurple;
  static const Color textDarkPrimary = Colors.black;
  static final Color snackBarDark = Colors.deepPurple.shade200;

  static const Color deleteButton = Colors.red;

  const AppColor._();
}
